name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: | 
          echo "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" | docker login ghcr.io -u RangaRathnayake --password-stdin
          docker build . --file Dockerfile --tag ghcr.io/rangarathnayake/loan-manage-back-nestjs:latest
          docker push ghcr.io/rangarathnayake/loan-manage-back-nestjs:latest
